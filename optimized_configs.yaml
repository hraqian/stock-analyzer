# Optimized per-regime configurations
# Generated by optimize.py
# Baseline avg tracking error: 48.16%
#
# To use: copy the strategy section of the desired regime into your
# config.yaml, or apply these overrides via the dashboard.

strong_trend:
  strategy:
    score_thresholds:
      short_below: 3.5
      hold_below: 6.0
    stop_loss_pct: 0.05
    take_profit_pct: 0.3
    percent_equity: 0.8
    rebalance_interval: 5
    atr_stop_multiplier: 4.0
  _meta:
    avg_tracking_error: 48.4
    tickers_tested:
    - AAPL
    - NVDA
    - MSFT
    - COST
    - LLY
    - KO
    - JNJ
    - TSLA
    - MARA
    - META
    - AMZN
    - GOOG
    - JPM
    - XOM
    avg_strategy_return: 12.26
    avg_buyhold_return: 54.62
